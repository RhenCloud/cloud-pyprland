# Cloud-Pyprland

RhenCloud's plugins for [pyprland](https://github.com/fdev31/pyprland)
## Nix 安装

本仓库提供了 `flake.nix`，可以直接作为 Nix 包安装：

```bash
nix profile install /path/to/cloud-pyprland
```

或者在仓库目录中构建：

```bash
nix build .
```
